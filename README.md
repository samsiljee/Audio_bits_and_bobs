Wishlist:

- Tone to filter function, take a tone, do FFT, use it in a density function to apply a filter to white-noise. i.e. turn additive synthesis into subtractive synthesis. This allows scope for tunable parameters, e.g. smoothing of the density profile
